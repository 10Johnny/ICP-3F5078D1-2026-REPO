apiVersion: v1
kind: Service
metadata:
  name: icp-devops-app
  namespace: icp-devops
spec:
  type: ClusterIP

  selector:
    app: icp-devops-app

  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
